import React from 'react';

const AddTodos = () => {
    return (
        <form>
            <input placeholder="add todos" />
            <button>add!</button>
        </form>
    );
}

export default AddTodos;